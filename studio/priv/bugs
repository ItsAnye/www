Fix Background color on buttons in header
When Rig is loaded, edit tab color slider doesn't change rig skin color
For selecting in explorer, rather than deselecting then selecting again (2 clicks), do it all in one click.
SpotLight's position settings are acting wierd.