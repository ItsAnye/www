BUILDINGS:
Make points not random
Add point snap to grid (Like a Coordinate Plane)
Add an actual grid/coordiante_plane to the background

Resize Explorer and Properties
Make a test feature which runs an iframe (or smth), seperate from visual editor (the thing you see while editing)!
When object is inserted, insert at random x + y position (integers only)
Insert Plane
Spotlight "fine line" property
Edit tab should only work if there is a rig loaded AND selected, not just loaded
Delete objects